from itertools import pairwise, count, permutations, combinations
from collections import Counter, defaultdict

'''
| 类别   | 工具             | 一行记忆口诀  | 典型赛题场景                                 |
| ---- | -------------- | ------- | -------------------------------------- |
| 🔥必背 | `pairwise`     | 相邻差分/坡度 | 连续上升/下降段、循环数组坡度                        |
| 🔥必背 | `accumulate`   | 前缀和/积   | 区间和、最小水位、股票回撤                          |
| 🔥必背 | `combinations` | 无序选 k   | 大小为 k 的子集暴力、两数/三数组合                    |
| 🔥必背 | `permutations` | 全排小数据   | n≤8 的暴力全排、字母映射                         |
| 🔥必背 | `product`      | 多循环笛卡儿  | 多钥匙多箱子、矩阵坐标枚举                          |
| 🔥必背 | `groupby`      | 连续分段    | 连续相同字符/数字段、最长平台                        |
| 🟡常考 | `islice`       | 滑窗切片    | 大数据取前 k 个、跳过表头                         |
| 🟡常考 | `takewhile`    | 条件前缀    | 最长前缀小于 x、股票连续涨                         |
| 🟡常考 | `dropwhile`    | 跳过前缀    | 去掉前导 0、前导空格                            |
| 🟡常考 | `chain`        | 拍平嵌套    | 多行多列合成一维、邻接表拍平                         |
| 🟡常考 | `count`        | 无限计数器   | 生成 0,1,2… 配合 `islice` 当索引              |
| 🟡常考 | `repeat`       | 无限重复    | 生成固定值流、配合 `zip` 填充                     |
| ⚪冷门  | `compress`     | 掩码过滤    | 布尔掩码选元素                                |
| ⚪冷门  | `filterfalse`  | 反向过滤    | 选不满足条件的                                |
| ⚪冷门  | `starmap`      | 参数解包    | 二维坐标 `starmap(lambda x,y:x*y, points)` |

    1、无限迭代器
        itertools.count(start, step)
        itertools.count(start, step)函数用于生成一个无限迭代的整数序列。它从 start 开始，并以 step 作为步长不断生成整数。

     2、组合生成器   
    
    itertools.permutations(iterable, r)
    itertools.permutations(iterable, r) 函数用于生成可迭代对象中所有长度为 r 的排列。


    itertools.combinations(iterable, r)
    itertools.combinations(iterable, r) 函数用于生成可迭代对象中所有长度为 r 的组合，不考虑元素顺序。
'''

'''
    python


'''

# 从 5 开始，步长为 2，生成整数序列
for i in count(5, 2):
    if i > 20:
        break
    print(i, end=' ')

# 定义一个列表
letters = ['a', 'b', 'c']

# 使用 permutations 生成排列
for perm in permutations(letters, 3):
    print(perm)

print('---------------')

# 使用 permutations 生成排列
for perm in combinations(letters, 3):
    print(perm)

a = [1, 2, 3, 4, 5]
for x, y in pairwise(a):
    print(x)
    print(y)
